rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /recipes/{recipeId} {
      allow read, write: if request.auth != null;
    }

    match /convives/{conviveId} {
      allow read, write: if request.auth != null;
    }

    match /menus/{menuId} {
      allow read, write: if request.auth != null;
    }
  }
}
